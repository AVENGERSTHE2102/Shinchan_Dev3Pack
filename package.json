{
  "name": "soldare-workspace",
  "version": "0.1.0",
  "private": true,
  "overrides": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm run start"
  },
  "dependencies": {
    "framer-motion": "^11.1.7"
  }
}
